<UserControl x:Class="TCPChatClient.Views.ConexionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TCPChatClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center">
        <TextBlock>Escribe la direccion IP del servidor:</TextBlock>
        <TextBox Text="{Binding IP}" Margin="0,5"></TextBox>
        <Button Command="{Binding ConectarCommand}" Margin="0,5">Conectar</Button>
    </StackPanel>

</UserControl>
